// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	site: 'https://bip-app.neocities.org',
	image: { responsiveStyles: true, layout: 'constrained' },
});
